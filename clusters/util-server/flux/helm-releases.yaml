apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  interval: 5m
  reconcileStrategy: Manual
  values:
    installCRDs: true
    replicas: 2
    dns01:
      enabled: true
    extraArgs:
      - --enable-cni
  interval: 5m