# MCPo is not deployed. The image `ghcr.io/open-webui/mcpo` is not
# available on a public registry as a stable, published Docker image.
#
# Until that changes, the Open WebUI deployment does not depend on MCPo.
# If/when an image is published, add a kustomization.yaml here based on
# the upstream docs and link it into `scripts/deploy-all.sh`.
